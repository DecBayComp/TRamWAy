
inferencemap package
--------------------

This is the top InferenceMAP package. It features ``if __name__ == '__main__':`` guarded code. 
If you are looking for documentation of the command line, go `there <commandline.rst>`.

The recommended entry point is :mod:`inferencemap.helper`.


Subpackages
-----------

.. toctree::

    inferencemap.helper
    inferencemap.io
    inferencemap.plot
    inferencemap.spatial
    inferencemap.tesselation
